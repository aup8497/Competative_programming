//i think this uses segment trees

// zz3.cpp
#include <bits/stdc++.h>
#define ll long long
#define MAX 100007
using namespace std;

ll n,m,i,j,k,x,res=0,sum=0;
int a[MAX],b[MAX];
std::vector<int> v;

int main(){

	cin >> n >> x;
std::vector< pair<int,pair<int,int> > > v;

	for(i=0;i<n;i++){
		cin >> u >> v >> w;
		v.push_back();
	}



return 0;	
}